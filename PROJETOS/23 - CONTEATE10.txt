algoritmo "conteate10"
//Programa básico que utiliza estrutura de repetição para contar de 0 a 10.

var
   num: inteiro

inicio
      enquanto (num <= 10) faca
               escreval(num)
               num <- num + 1
      fimenquanto
      escreval("Fim da conta.")

fimalgoritmo