algoritmo "conteate0"
//Programa básico que utiliza estrutura de repetição para contar de 10 a 0.

var
   num: inteiro

inicio
num <- 10
      enquanto (num >= 0) faca
               escreval(num)
               num <- num - 1
      fimenquanto
      escreval("Fim da conta.")

fimalgoritmo